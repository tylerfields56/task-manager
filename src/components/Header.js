import React from 'react';

const Header = () => {
  return (
    <header style={{ marginBottom: '30px' }}>
      <h1 style={{ fontSize: '2rem', color: '#333' }}>Task Manager</h1>
      <p style={{ color: '#666' }}>Organize your tasks effectively.</p>
    </header>
  );
};

export default Header;